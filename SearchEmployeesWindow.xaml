<UserControl x:Class="WpfApp1.SearchEmployeesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
      
        Width="785" Height="550"
      
        Background="Transparent"
       
        >

    <Border CornerRadius="10">
        <Border.Background>
            <SolidColorBrush Color="Transparent"/>
        </Border.Background>
        <Border CornerRadius="10"
BorderThickness="1">

            <Border.BorderBrush>
                <SolidColorBrush Color="Transparent"/>
            </Border.BorderBrush>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="412*"/>
                    <ColumnDefinition Width="371*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="1" Margin="5,5,5,5" Grid.ColumnSpan="2">
                    <Grid Margin="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Search Employees" FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,-56,0,76"/>

                        <Grid Margin="0,10,0,62" Grid.RowSpan="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Left Column -->
                            <StackPanel Grid.Column="0" Margin="0,0,20,0">
                                <TextBox x:Name="IdTextBox" Margin="0,0,0,20" Style="{StaticResource RoundTextBox}" Tag="Id"/>

                                <TextBox x:Name="FirstNameTextBox" Margin="0,0,0,20" Style="{StaticResource RoundTextBox}" Tag="First Name"/>

                                <ComboBox x:Name="PositionComboBox" Margin="0,0,0,20" SelectedIndex="0" Style="{StaticResource StandardComboBox}">
                                    <ComboBoxItem Content="Select Position" IsEnabled="False" />
                                    <ComboBoxItem Content="Manager"/>
                                    <ComboBoxItem Content="Admin"/>
                                    <ComboBoxItem Content="Developer"/>
                                    <ComboBoxItem Content="Designer"/>
                                    <ComboBoxItem Content="Consultant"/>
                                    <ComboBoxItem Content="Intern"/>
                                </ComboBox>

                                <TextBox x:Name="EmailTextBox" Margin="0,0,0,20" Style="{StaticResource RoundTextBox}" Tag="Email"/>
                            </StackPanel>

                            <!-- Right Column -->
                            <StackPanel Grid.Column="1">
                                <TextBox x:Name="JoinDateTextBox" Margin="0,0,0,20" Style="{StaticResource RoundTextBox}" Tag="Joined Date"/>

                                <TextBox x:Name="LastNameTextBox" Margin="0,0,0,20" Style="{StaticResource RoundTextBox}" Tag="Last Name"/>

                                <ComboBox x:Name="DepartmentComboBox" Margin="0,0,0,20" Style="{StaticResource StandardComboBox}" SelectedIndex="0">
                                    <ComboBoxItem Content="Select Department" IsEnabled="False" />
                                </ComboBox>

                                <ComboBox x:Name="GenderComboBox" Margin="0,0,0,20" Style="{StaticResource StandardComboBox}">
                                    <ComboBoxItem Content="Select Gender" IsEnabled="False" IsSelected="True"/>
                                    <ComboBoxItem Content="Male" />
                                    <ComboBoxItem Content="Female"/>
                                </ComboBox>

                            </StackPanel>
                        </Grid>

                        <!-- Result Container -->
                        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,64,0,0" Grid.RowSpan="2">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <ItemsControl x:Name="EmployeeDetailsContainer"/>
                            </ScrollViewer>
                        </Border>

                        <!-- Search Button -->
                        <Button Grid.Row="3" Content="Search" Width="100" Height="30" HorizontalAlignment="Right" Margin="0,20,0,0" Click="SearchButton_Click" Foreground="White"  Background="#4C2BB0" BorderBrush="black"/>
                        <Button Grid.Row="3" Content="Back" Width="100" Height="30" HorizontalAlignment="Right" Margin="0,20,118,0" Click="backButton_Click" Foreground="White"  Background="#4C2BB0" BorderBrush="black"/>
                    </Grid>
                </Grid>
            </Grid>
        </Border>
    </Border>
</UserControl>
