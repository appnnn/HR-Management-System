<UserControl x:Class="WpfApp1.ApproveRequestsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        
        Width="785" Height="550"
       
        Background="Transparent"
       >

    <UserControl.Resources>
        <Style x:Key="RoundedDataGridStyle" TargetType="DataGrid">
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGrid">
                        <Border CornerRadius="10" Background="{TemplateBinding Background}">
                            <ScrollViewer x:Name="DG_ScrollViewer">
                                <ScrollViewer.Template>
                                    <ControlTemplate TargetType="ScrollViewer">
                                        <Grid>
                                            <ContentPresenter />
                                        </Grid>
                                    </ControlTemplate>
                                </ScrollViewer.Template>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border CornerRadius="10">
        <Border.Background>
            <SolidColorBrush Color="Transparent"/>
        </Border.Background>
        <Border CornerRadius="10"
        BorderThickness="1">

            <Border.BorderBrush>
                <SolidColorBrush Color="#CCCCCC"/>
            </Border.BorderBrush>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0"/>

                <Grid Grid.Row="1" Margin="5">
                    <Grid Margin="5">
                        <DataGrid x:Name="RequestsDataGrid" SelectionChanged="RequestsDataGrid_SelectionChanged" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" Margin="0,12,0,58" Background="White" BorderThickness="0" BorderBrush="#7D7D7D" ClipToBounds="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                                <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="100"/>
                                <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="100"/>
                                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="150"/>
                                <DataGridTextColumn Header="Department" Binding="{Binding Department}" Width="100"/>
                                <DataGridTextColumn Header="Position" Binding="{Binding Position}" Width="100"/>
                                <DataGridTextColumn Header="Join Date" Binding="{Binding JoinDate}" Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <TextBlock x:Name="NoRequestsText"
                                Text="No requests to show"
                                FontStyle="Italic"
                                Foreground="Gray"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Visibility="Collapsed"/>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,0,5">
                            <Button x:Name="ApproveButton" Content="Approve" Width="100" Margin="5" Click="Approve_Click" Style="{StaticResource DeBtn}" IsEnabled="False" Background="#4C2BB0" BorderBrush="black"/>
                            <Button x:Name="RejectButton" Content="Reject" Width="100" Margin="5" Click="Reject_Click" Style="{StaticResource DeBtn}" IsEnabled="False" Background="#4C2BB0" BorderBrush="black"/>
                        </StackPanel>
                    </Grid>
                </Grid>
                <TextBlock Grid.Row="0" Text="Pending New Employee Requests" FontSize="24" FontWeight="Bold" 
                    HorizontalAlignment="Left" Margin="223,7,0,413" Grid.RowSpan="2"/>
            </Grid>
        </Border>
    </Border>
</UserControl>
